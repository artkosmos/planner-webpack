import React from 'react';
import { ListCreator } from '@/components/business';

const Home = () => {
  return (
    <div className={'container'}>
      <ListCreator />
    </div>
  );
};

export default Home;
