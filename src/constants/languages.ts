import { TAvailableLanguages } from '@/common/types';

export const languages: TAvailableLanguages[] = ['en', 'ru'];

export const dateFormats = {
  en: 'MM/DD/YYYY hh:mm:ss A',
  ru: 'DD.MM.YYYY HH:mm:ss',
};
